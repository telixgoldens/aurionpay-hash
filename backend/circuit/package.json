{
  "name": "aurionpay-circuit",
  "private": true,
  "scripts": {
    "i": "pnpm i",
    "compile": "circom withdraw.circom --r1cs --wasm --sym -o build",
    "setup": "snarkjs groth16 setup withdraw.r1cs powersOfTau28_hez_final_12.ptau withdraw_0000.zkey",
    "contribute": "snarkjs zkey contribute withdraw_0000.zkey withdraw_final.zkey --name=\"aurionpay\" -v",
    "vkey": "snarkjs zkey export verificationkey withdraw_final.zkey verification_key.json",
    "verifier": "snarkjs zkey export solidityverifier withdraw_final.zkey Verifier.sol",
    "all": "npm run compile && npm run setup && npm run contribute && npm run vkey && npm run verifier"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.6"
  }
}
